apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "reminder-app.fullname" . }}-test-connection"
  labels:
    {{- include "reminder-app.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "reminder-app.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
